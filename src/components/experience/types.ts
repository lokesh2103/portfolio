export interface ExperienceType {
  title: string;
  company: string;
  companyLogo: string;
  period: string;
  description: string;
  achievements: string[];
}

export interface SkillType {
  name: string;
  icon: string;
  level: number;
}